<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AIPFoodLookup.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AIPFoodLookup"
    Title="AIPFoodLookup">

    <TabBar>
        <ShellContent Title="Search"
                     Icon="search_strong_icon"
                     ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Catgegories"
                     Icon="catalog_icon"
                     ContentTemplate="{DataTemplate local:CatagoriesPage}" />
    </TabBar>
</Shell>
