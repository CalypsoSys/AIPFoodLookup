<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AIPFoodLookup.MainPage"
             xmlns:viewmodel="clr-namespace:AIPFoodLookup.ViewModel"
             x:DataType="viewmodel:MainViewModel">

    <Grid RowDefinitions="100, Auto, *, *" ColumnDefinitions=".75*, .25*" Padding="10" RowSpacing="10" ColumnSpacing="10">
        <Image Grid.ColumnSpan="2" Source="adaptive_icon.png" />

        <Entry Placeholder="Enter a food to check" Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Text, Mode=TwoWay}"  />

        <CollectionView Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Allowed}" >
        </CollectionView>
        
        <CollectionView Grid.Row="3" Grid.ColumnSpan="2" ItemsSource="{Binding NotAllowed}" >
        </CollectionView>
    </Grid>

    
</ContentPage>
